// repeated value mai se sirf ek single value de from an array
#include<iostream>
#include<vector>
using namespace std;
 int singleNonDuplicate(vector<int>& nums) {
        int st = 0 , end = nums.size()-1;
        while(st<=end){
            int mid = st + (end-st)/2;
        }
    }
int main(){
    
    return 0;
}