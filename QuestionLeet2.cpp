#include<iostream>
#include<vector>
#include<climits>
#include<functional>
#include<deque>
#include <unordered_map>
using namespace std;

 
                    
                    if (diff > 2) break; // early stop
                }
                
                if (diff <= 2) {
                    result.push_back(q);
                    break; // no need to check further dictionary words
                }
            }
        }
        
        return result;
    }
   
  
  
int main(){

  return 0;
 }
   
